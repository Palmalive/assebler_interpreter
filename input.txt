// тестовая программа на ассемблере
mov rax, 11          // помещаем в rax число 11
mov rbx, 22          // помещаем в rbx число 22
add rcx, rax, rbx      // складываем rax и rbx и помещаем результат в rcx
add rcx, rcx, 2      // складываем rcx и число 2 и помещаем результат в rcx